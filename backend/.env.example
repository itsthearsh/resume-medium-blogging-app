DATABASE_URL="prisma+postgres://..."

# Origin database — required only if DATABASE_URL is an Accelerate wrapper
# around an existing database (migrations can't run through Accelerate).
# Not needed for native Prisma Postgres.
DIRECT_DATABASE_URL="postgres://user:password@host:port/dbname?sslmode=require"
